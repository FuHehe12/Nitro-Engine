# Translation Skill Configuration

> Modify these settings via maintenance mode (`/translate` without a target file → select "Configuration").

| Setting | Current Value | Description |
|---------|---------------|-------------|
| `keep_intermediate` | FALSE | Whether to keep intermediate files (glossary, translation brief, tone sample, split segments) after translation completes. When set to TRUE, a file list is shown and user confirmation is required before cleanup. |
